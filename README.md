# speculative-decoding-from-scratch-in-pytorch
Chapter 5.2 of Inference Engineering, built and measured. Train a small causal transformer with a KV cache as the target model and a smaller one as the draft, then implement the three ways the book describes to get more than one token out of every target forward pass. Draft-target speculation with the exact rejection-sampling verification, which…
